//WAP to input a string and display the same.
#include <stdio.h>

int main(){
    char str[50];
    printf("Enter a string");
    scanf("%s", &str);

    printf("Given string is %s", str);

    return 0;
}